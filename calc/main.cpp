#include <iostream>
#include <vector>
#include <map>

using namespace std;
int findAllFactors(multimap<long, vector<long> >& resMap, int iInt)
{
    for(int i = 1; i <= iInt; i++)
    {
        
    }
}
int calcMax(multimap<long, vector<long>>& resMap)
{
}


int main(int argc, char* argv[])
{
    vector<long> resVec;
    multimap<long, vector<long> > resMap;
    int iInt = 72;
    findAllFactors(resMap, 72);
    calcMax(resMap); 
}
